# Application Requirements

## 当前范围

- 维护用户定义的用例接口、用例编排和出站端口。
- 当前仅依赖 `domain`，没有业务用例或公开契约。

## 验收基线

- 只通过 `domain` 公开出口调用领域能力。
- 用例覆盖主路径、失败路径和用户给出的验收示例。
- 公开接口、DTO、事务意图和错误语义变化同步契约索引与直接调用方。

## 待用户确认

- 第一条业务用例、输入输出 DTO、出站端口和事务边界。
