# Domain Requirements

## 当前范围

- 维护用户定义的领域对象、值对象、业务规则、领域错误和状态机。
- 当前仅有包边界，没有业务类型或公开契约。

## 验收基线

- 领域代码保持纯 Java，不依赖其它项目模块或 I/O 实现。
- 用户定义的规则、输入、输出和失败语义有对应纯单元测试。
- 公开类型变化同步契约索引和 code map。

## 待用户确认

- 第一批领域对象、规则、状态机和验收示例。
