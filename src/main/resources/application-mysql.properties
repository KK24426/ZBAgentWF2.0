# 仅当 SPRING_PROFILES_ACTIVE=mysql 或 JVM -Dspring.profiles.active=mysql 时加载。
# URL、账号和密码由环境变量 SPRING_DATASOURCE_* 或外部本地配置提供。
mybatis.mapper-locations=classpath*:/mapper/**/*.xml
spring.sql.init.mode=never
